Frontend Preview

Build the app:

```bash
cd frontend
npm run build
```

Preview the production build locally:

```bash
cd frontend
npm run preview
```

The preview server serves the compiled `dist` output so you can verify the production bundle in a browser.